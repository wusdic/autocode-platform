# autocode-platform 综合修正方案（合并两份评审 + 真机报告）

> 输入：① 真机部署验证报告（全流程跑通，156/156 测试）；② 评审 A（逐行级，ZHIPU）；
> ③ 评审 B（结构化，GPT）。本文是三者合并去重后的**权威修正方案**，并记录落地状态。
>
> 状态：✅ 本轮已改并推送　·　📋 已记录待后续（阶段 13 生产化）
>
> 核心认知：平台架构与流程**真机已验证可跑通**；阻碍"开箱即用"的是一批 **Hermes v0.16
> 命令/配置格式细节**——静态分析看不出、只有真机暴露。两份评审一致命中的根因都已修。

---

## 一、🔴 P0 阻断（不改无法跑通）——✅ 全部已改

| # | 问题 | 文件 | 改法 | 状态 |
|---|---|---|---|---|
| NEW-E | swarm `--workers`（v0.16 无）→ 单数可重复 `--worker PROFILE:TITLE` | `control_plane.py` `swarm()` | for 循环逐个 `--worker f"{w}:{w}"` | ✅ |
| NEW-K/L | `toolsets` 逗号串无效 + 内置工具默认全开 + CEO 拿到 `code_execution` 直接写码 | `launch_project.sh` | 值改 **JSON 数组**；`agent.disabled_toolsets` 含 **`code_execution`** | ✅ |
| NEW-M/N | `terminal.docker_volumes` bare string → 容器起不来 | `launch_project.sh` | 改 **JSON 数组** `["host:container"]` | ✅ |
| NEW-O | 插件只复制不 enable → 第二层闸门全程缺席 | `launch_project.sh` | `hermes plugins enable policy` + 校验 + `HERMES_ACCEPT_HOOKS=1` | ✅ |
| NEW-I | systemd `gateway start` 需已 install 服务 → 无限重启 | `launch_project.sh` | `gateway run`（前台，Type=simple 正确） | ✅ |
| 占位模型 | 17 角色用不存在的模型串、不设 provider/base_url | `launch_project.sh` | `create_role` 增 `provider/base_url`；默认 zai×deepseek，**全可环境变量覆盖**；启动前校验 key | ✅ |
| NEW-F | `monitor.sh` 用不存在的 `config get` | `monitor.sh` | 改 `config show \| grep`，判据 `code_execution` | ✅ |

> 两个大模型：**GLM-5.2（默认）+ DeepSeek**。zai 角色用 `glm-5.2`，跨供应商质疑角色（critic/security/qa/change-guardian）用 `deepseek-chat`。若 z.ai 账号无 glm-5.2，用 `ZAI_PRIMARY_MODEL`/`ZAI_SECONDARY_MODEL` 环境变量改，无需动脚本。

## 二、🟠 P1 稳定性 ——✅ 已改

| # | 问题 | 改法 | 状态 |
|---|---|---|---|
| NEW-H | 密钥脱敏致 `PLATFORM_TOKEN` 传不进、重启回落 change-me → 401 | `control_plane.py` token 支持 `~/platform/.platform_token` 文件 fallback | ✅ |
| NEW-P/26.3/26.4 | 控制平面用 nohup 不稳、systemd 缺 PATH/token | 部署脚本装 `autocode-control-plane.service`（Restart=always + 固定 token + PATH 含 hermes venv + XDG） | ✅ |
| NEW-J | gateway/控制平面 systemd 缺 `XDG_RUNTIME_DIR` → "No medium found" | unit 加 `XDG_RUNTIME_DIR=%t`；脚本 `export XDG_RUNTIME_DIR` | ✅ |
| §26.1 | Docker 以 root 写产物，宿主用户读不了 | ① 非 root 沙箱镜像 `docker/python-sandbox.Dockerfile`（映射宿主 UID）+ 部署构建 + launcher 使用；② `monitor.sh` chown 兜底告警 | ✅ |
| gateway env | 每项目 gateway unit 缺 key/PATH/hooks | unit 加 `EnvironmentFile=-~/.hermes/.env`、`PATH`、`HERMES_ACCEPT_HOOKS=1`、`WorkingDirectory` | ✅ |

## 三、🟡 P2 正确性 / 安全 ——✅ 已改

| # | 问题 | 改法 | 状态 |
|---|---|---|---|
| KNOWN-04 | 架构委员会 swarm 未代码触发（违背"不靠自觉"） | `POST /architecture-swarm` 端点 + `watchdog.sh`"PRD 在、ADR 不在则自动起"（marker 去重） | ✅ |
| 路径越界 | dev-worker 路径只做字符串前缀，未防绝对路径/`..`/symlink 逃逸 | `policy_plugin.normalize_target()` 统一 resolve 到 workspace 内，越界即 block | ✅ |
| QA/release 越权 | qa/release 是执行角色但无写入范围限制 | qa 仅可写 `tests/`、release 仅可写 `dist/`，不得改业务代码 | ✅ |
| 资源/限流 | 低配机器并发高易 429/OOM | `<4` 核默认 `kanban.max_in_progress=1`；monitor 分级告警余额耗尽(1113)/临时过载(1305) | ✅ |
| 主机预检 | 部署不提示资源不足、Docker Hub 超时 | `00-host-setup.sh` 加资源预检 + `DOCKER_REGISTRY_MIRROR` 支持 | ✅ |

## 四、🛡️ CI 加固（治本）——✅ 已改

> 根因：`FakeGateway` 把所有 CLI 调用 mock 掉 → CLI 漂移零覆盖，"单测全绿却部署崩溃"。

- 新增 `tests/test_static_contracts.py`：断言仓库**不存在** `--workers`、`config get`、占位模型；**必须存在** `model.provider/base_url`、`plugins enable policy`、`HERMES_ACCEPT_HOOKS`、`gateway run`、JSON 数组配置。— ✅
- 新增 `test_swarm_cmd_uses_singular_worker_flag`：直接打真实 `HermesGateway.swarm`（monkeypatch subprocess），不走 FakeGateway，验证构造的 cmd 用单数 `--worker`。— ✅
- 文档：`01/02/03` + 验证矩阵全部同步（`--worker`、`config show`、模型环境变量、gateway run、JSON 配置、plugins enable 行）。— ✅

## 五、📋 已记录、待后续（阶段 13 生产化）

| 项 | 说明 |
|---|---|
| 完整状态机 orchestrator | 产品→架构→dev→QA→release 全程事件驱动（现用 watchdog 轮询兜底架构衔接） |
| `ProjectRegistry` 换 Postgres | 现为内存 + `rehydrate` + `.ports.json` 落盘；生产持久化 + 审计字段 |
| `/events` 持续 SSE | 现为一次性快照；需 Redis 事件总线 |
| `task_scope.<id>.yaml` 结构化 | 把 task 绑定到具体 `design_version`，防旧 approved 被新变更借用 |
| 鉴权兼容 `Authorization: Bearer` | 现仅 `X-Token`（够用）；可加 Bearer 兼容降低调试误用 |
| nginx/Caddy TLS + 备份 | 控制平面对外加 TLS；定期备份 `/data/projects/{id}` |

## 六、两份评审的差异取舍（说明）

- **toolsets 写法**：评审 A 建议用 PyYAML 直接 patch `config.yaml`；本方案采用**更轻的 JSON 数组串 `config set`**——与真机已验证可行的 `docker_volumes` JSON 写法一致，且不引入 PyYAML 依赖。
- **模型默认值**：按需求定为**默认 `glm-5.2` + DeepSeek**（两个大模型）。若 z.ai 账号无 glm-5.2，经 `ZAI_PRIMARY_MODEL`/`ZAI_SECONDARY_MODEL` 环境变量回退到 glm-5.1，无需动脚本。
- **ENV-02 预置空 `approved_versions.txt`**：评审 A 建议 `touch` 解死锁。**不采纳**——空文件在本插件逻辑里仍是"无批准版本"（按非空行判定），`touch` 不解锁且会误导；正确做法是设计阶段产出后再开闸（《03》Step 7）。dev-worker 本就应在设计批准后才开工，正常流程无死锁。

## 七、第二次真机报告（2026-06-18，全流程跑通）新增修复

这次部署**完整跑通了全自动链路**（需求→PRD→ADR→todo.py 501 行→138 测试通过），但暴露 3 处需改：

| # | 问题 | 文件 | 改法 | 状态 |
|---|---|---|---|---|
| 真机-P0 | 插件校验 `hermes plugins list \| grep -q policy` 在 `set -euo pipefail` 下因 **SIGPIPE(141)** 误判未启用 → **建项目 100% 失败** | `launch_project.sh` | 改查 `hermes plugins enable policy` **自身输出**（`case *enabled*`，无管道）；静态契约测试禁止该反模式 | ✅ |
| 真机-P1 | `monitor.sh` 用 `config show \| grep disabled_toolsets` 校验权限，但 **`config show` 不渲染该字段** → 持续误报 CRIT | `monitor.sh` | 改**直接读 `profiles/ceo/config.yaml`**；同时把 `printf \| grep -q` 全改 here-string（防同类 SIGPIPE 漏报）；《03》Step 8-2 同步 | ✅ |
| 真机-P1 | `approvals.mode` 默认 **manual** → 无人值守流程被"command approval"反复打断 | `launch_project.sh` + gateway 单元 | 项目配 `approvals.mode=off`（可 `HERMES_APPROVALS_MODE` 覆盖）+ gateway 单元 `HERMES_YOLO_MODE=1`；安全模型见验证矩阵 §G | ✅ |

> **安全说明**：off/yolo **不降低实际安全**——HARDLINE 12 条（`rm -rf /`/`mkfs`/`dd`/fork bomb…）+ sudo-stdin guard 仍不可绕过；CEO 无终端、dev-worker 在 Docker（源码级豁免 approval）。安全靠架构隔离 + 设计闸门 + 监测实现，不靠人工审批（无人值守系统没人审批）。详见 `docs/hermes-验证矩阵.md` §G。

## 八、第三轮：两份外部评审（2026-06-19）的采纳

两份评审基于 commit `232ec9b`（在回流 commit `3913204` **之前**），故其头号结论"3 个修复没回流、新部署 100% 卡 Step 4"**已不成立**——SIGPIPE 插件校验、monitor 读 config.yaml、approvals/yolo 均已在 `3913204` 回流。两份**新增且一致**的有价值点，本轮采纳：

| 来源 | 建议 | 判定 | 落地 |
|---|---|---|---|
| 两份都点名 | **沙箱镜像静默回退 `python:3.11-slim` = 安全漏洞**（off/yolo 的安全前提是 dev-worker 在非 root 加固容器，回退公共 root 镜像就破坏了这个前提） | ✅ 强成立 | launcher 默认**拒绝回退**，需 `ALLOW_PUBLIC_SANDBOX_FALLBACK=1` 才降级；deploy 镜像构建失败默认 `exit 1` |
| 评审 1 §5 | **QA gate 不能把失败测试口头合理化**（报告"138 pass 2 fail 但 Step8 全过"） | ✅ 强成立，直击设计目标 | 新增 `reports/qa/status.json` 契约：QA 写、release 读；**policy 插件硬拦**——无 `release_allowed:true` 时 release 的 terminal/写文件全 block；SOUL.qa/release 同步 |
| 两份都提 | **无人值守审批做成受控开关**，不无条件 off | ✅ 成立 | `AUTOCODE_UNATTENDED`（默认 1，因平台本就无人值守）统一控制 `approvals.mode` + 网关 `HERMES_YOLO_MODE`；审批配置只写**本项目 HERMES_HOME**（不动用户主配置） |
| 评审 1 §8 | 部署韧性：Docker 安装 fallback、pip 超时重试 | ✅ 低风险快赢 | `00-host-setup.sh` get.docker.com 失败回退 `docker.io`；`01-deploy` pip 加 `--timeout/--retries` + 可选 `PIP_INDEX_URL` |

**未采纳/留作阶段 13（附理由）**：
- **orchestrator.py 完整状态机 / Postgres 注册表 / Redis 持续 SSE**：均属阶段 13 生产化，风险大、依赖外部组件；当前 `watchdog.sh` 的"PRD→ADR 自动起 + marker 去重"已是可用兜底，已在 §五/F 记录。
- **approvals off 的审计落盘（评审 2 §4.1）**：`approvals.audit_log` 配置键未在官方文档核实，不盲加；改为依赖 monitor 的 journald 日志检查兜底。
- **dev-worker-2 默认切 DeepSeek（评审 §9）**：与用户明确指定的"默认 GLM-5.2"冲突，不改默认；模型已全可经 `*_MODEL` 环境变量覆盖。
- **monitor HERMES_HOME 硬编码（评审 2 §3）**：误判——`monitor.sh` 早已 `for proj_dir in ${PLATFORM_DATA_ROOT}/*/` 遍历所有项目。

## 九、自动化（无人值守）稳健性加固

针对"这一版自动化还有什么问题"的分析，落地 4 条低风险加固（其余 #1 review 自动放行、#3 yolo 是否绕过 hook、#6 完整 orchestrator 属设计取舍/真机/阶段13）：

| # | 风险 | 改法 | 文件 |
|---|---|---|---|
| #2 | 启用 policy 后，若 Hermes 不向 hook 传 `task_id` → dev-worker 写入**全锁**、平台停摆 | 设计闸门**降级兜底**：拿不到 task_id/缺 allowed_paths 时，默认允许写 workspace 内、但禁 `design/`、禁越界（normalize 兜底）；`POLICY_REQUIRE_TASK_ID=1` 可强制严格 | `policy_plugin.py` |
| #5 | 永久失败任务被 watchdog **无限续跑** | 每项目续跑计数 + `MAX_CONTINUATIONS`(默认20) 熔断 → 建"人工 review"卡，停自动续 | `watchdog.sh` |
| #4 | 429/限流无自动退避，持续打满同一供应商 | monitor 检测 1305 写 `.provider_pause`(until epoch)；watchdog 暂停期内**不起新 swarm** | `monitor.sh` + `watchdog.sh` |
| #7 | 磁盘中途写满 → 任务失败 | 建项目前 <5GB 拒绝(`AUTOCODE_ALLOW_LOW_DISK=1` 跳过)；monitor <2GB CRIT | `launch_project.sh` + `monitor.sh` |
| #1 | dev-worker `review-required` 自我阻断需**人工** `kanban complete` 放行 → 非真正零人工 | **可选开关** `AUTOCODE_AUTO_APPROVE_REVIEW=1`：watchdog 自动放行（实现零人工无人值守）。**默认关**——自动放行会跳过人工代码评审，仅在信任 QA gate + 设计闸门时开启 | `watchdog.sh` |

> 仍需真机确认的两条命门（本容器给不了）：**`task_id` 是否真到达 pre_tool_call hook**、**`HERMES_YOLO_MODE`/Docker 豁免是否也绕过 hook**——由《03》Step 8-5 验证。#2 的降级兜底正是为前者兜底，避免真机一旦 task_id 不到就整盘停摆。

## 十一、阶段 13 ①：Orchestrator 状态机（已落地）

把"正常业务编排"从 watchdog 文件轮询升级为**幂等状态机** `platform/orchestrator.py`：
- 状态存 `workspace/.autocode/state.json`；每 tick 读 文件信号 + Kanban 快照，幂等推进：
  `PRD.md→架构swarm`→`ADR.md+approved→dev-lead切卡`→`所有dev卡done→QA卡`→`status.json release_allowed→release卡`→`release done→complete`。
- 复用 `HermesGateway`（swarm/kanban_create/kanban）；尊重 `.provider_pause`（限流暂停不起新 swarm）。
- **watchdog 退回只管异常**（续跑/熔断/review 放行），原 PRD→ADR 兜底已移入 orchestrator（避免双触发）。
- 部署：`scripts/01-deploy-platform.sh` 自动 cp；`03` Step 9 cron 加 `orchestrator.py tick --all`。
- 测试：`tests/test_orchestrator.py`（8 例）注入 FakeGateway + 临时 workspace 覆盖每个跃迁 + 幂等 + 限流暂停 + tick_all 扫描，**不依赖真实 Hermes**。

> 阶段 13 后续：② Registry 抽象 + Postgres、③ Redis 事件总线 + 持续 SSE、④ TLS/备份/成本。

## 十二、第二轮端到端真机报告（2026-06-19，对最新代码）

**好消息**：这轮测的是含 orchestrator + QA gate + #2 降级兜底的最新代码——**8/8 任务零人工跑通，39 测试全绿，`release_allowed:true`**，orchestrator 状态机/QA gate/降级兜底全部按预期工作。报告发现 3 个真 bug + **解答了两条命门**：

| 项 | 内容 | 处理 |
|---|---|---|
| **Bug-1 P0** | `config set terminal.docker_volumes '[...]'` 被存成 YAML **字符串标量** → DockerEnvironment 当非 list 静默丢弃 → 容器无卷挂载 → runc "cwd outside mount namespace"。**这推翻了上一轮"JSON 数组串可行"的结论** | 改 `set_docker_volumes()` 用 **pyyaml 直接写 config.yaml 为真正 YAML 列表**；`requirements.txt` 加 `pyyaml`；02 同步 |
| **Bug-2 P1** | 磁盘阈值 5GB 太高（1.9GB 也能跑，项目仅需 ~250MB） | `AUTOCODE_MIN_DISK_GB` 默认 5→**2**（与 monitor CRIT<2 对齐） |
| **Bug-3 P2** | dev-worker 用 git worktree，但 workspace 未 `git init` | launcher 建 workspace 后 `git init + add + commit` |
| **命门①解答** | pre_tool_call hook 在 **kanban-worker + Docker + yolo** 下**确实触发**（gate(a) 拦截成功）→ #25204 对本平台不成立 | 矩阵 D + B 改 ✅ |
| **命门②解答** | Hermes **不传 task_id 给 hook**（`HERMES_KANBAN_TASK`/kwargs 都没有）→ 第三道闸无法生效 | 已由 #2 降级兜底（项目级）覆盖，**主流程不受影响**；矩阵 B + 01 §6.2 标注 |

> 结论：之前担心的两条 hook 命门——**hook 会触发**（好），**task_id 不传**（坏但已兜底）。平台安全实际靠 第一层 disabled_toolsets + 第二层 approved_designs + 项目级路径降级，三者真机验证有效。

## 十三、验收（本轮）
- `make ci`：**63 测试全过** + 6 脚本 `bash -n` + 文档代码块校验 + 静态契约（再增 3 条：沙箱回退护栏、无人值守开关、QA gate）。
- 两次真机已端到端跑通全链路；本轮把两份评审里"超出已修范围、且服务设计目标（无人值守 + 真隔离 + QA 阻断）"的项落地。
